import 'package:flutter/material.dart';

const Widget searchApi = SizedBox(
    // LocationPermission permission = await Geolocator.checkPermission();
    // Map<String, dynamic> requestBody = {};
    // if ((permission == LocationPermission.whileInUse || permission == LocationPermission.always) && await Geolocator.isLocationServiceEnabled()) {
    //   Position currentPosition = await Geolocator.getCurrentPosition();
    //   requestBody = {
    //     "textQuery": searchQuery,
    //     "includedType": "locality",
    //     "strictTypeFiltering": "true",
    //     "locationRestriction": {
    //       "circle": {
    //         "center": {"latitude": currentPosition.latitude, "longitude": currentPosition.latitude},
    //         "radius": 500.0
    //       },
    //     },
    //   };
    // } else {
    //   requestBody = {
    //     "textQuery": searchQuery,
    //     "includedTypes": ["locality"],
    //   };
    // }
    // final Map<String, String> headers = {
    //   'Content-Type': 'application/json',
    //   'X-Goog-Api-Key': 'AIzaSyD-zFDSMo3Otg25k6NZ0bMZC2xCPIsof5c',
    //   'X-Goog-FieldMask': 'places.displayName,places.formattedAddress,places.location',
    // };
    // final http.Response response = await http.post(
    //   Uri.parse('https://places.googleapis.com/v1/places:searchText'),
    //   headers: headers,
    //   body: jsonEncode(requestBody),
    // );
    // print(response.body);
    );
