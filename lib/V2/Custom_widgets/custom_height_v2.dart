import 'package:flutter/material.dart';

class CustomHeightV2 extends StatelessWidget {
  const CustomHeightV2({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 18,
    );
  }
}
